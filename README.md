# FileConverter

Universal file-to-DataFrame converter for data exploration.

## Features
- JSON / CSV / Excel / Pickle / Parquet input
- Unified DataFrame pipeline
- Export to Parquet or CSV
- CLI + Jupyter workflow

## Example
```bash
  python cli.py data/input/events.json --preview --drop-empty
```
