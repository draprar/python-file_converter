[project]
name = "tabular-convert"
version = "0.1.0"
description = "Universal tabular format converter"
dependencies = [
    "pandas",
    "openpyxl",
    "pyarrow"
]

[project.scripts]
convert = "file_converter.cli:main"